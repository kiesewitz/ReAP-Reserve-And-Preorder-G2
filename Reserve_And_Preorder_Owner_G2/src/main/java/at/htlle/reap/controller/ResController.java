package at.htlle.reap.controller;
/*
import at.htlle.reap.model.Reservation;
import at.htlle.reap.service.ResService;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/reservations")
public class ResController {

    private final ResService resService;

    public ResController(@RequestBody ResService resService) {
        this.resService = resService;
    }

    @GetMapping
    public List<Reservation> getAllReservations() {
        return resService.getAllReservations();
    }

    @GetMapping
    public Reservation getReservationById(@RequestParam("id") long id) {
        return resService.getReservationById(id);
    }



}
*/